toss:
  # 토스페이먼츠 시크릿 키 (Basic Auth ID 로 사용)
  secret-key: ${TOSS_SECRET_KEY:}
  # 프론트에서 사용하는 클라이언트 키 (서버에선 보통 참조만 함)
  client-key: ${TOSS_CLIENT_KEY:}
  # 토스페이먼츠 API 기본 URL
  base-url: https://api.tosspayments.com
